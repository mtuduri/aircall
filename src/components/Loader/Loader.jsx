import React from 'react';
import { Loading, LoadingContainer } from './Loader.styles.jsx';

const Loader = () => {
  return (
    <LoadingContainer>
      <Loading />
    </LoadingContainer>
  );
};
export default Loader;
